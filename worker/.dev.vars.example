# Development Environment Variables
# Copy this to .dev.vars if needed for local development

# Cloudflare Browser Rendering API (required for production screenshots)
# Get these from: https://dash.cloudflare.com/profile/api-tokens
CLOUDFLARE_ACCOUNT_ID=your_account_id_here
CLOUDFLARE_API_TOKEN=your_api_token_here

# Alternative: Use local Puppeteer (automatically fallback if API fails)
# Set to "puppeteer" to force Puppeteer even if API credentials are available
# SCREENSHOT_BACKEND=puppeteer

# Google Gemini API key (Phase 4)
# GEMINI_API_KEY=your_key_here

# Custom timeout values (milliseconds)
# SCREENSHOT_TIMEOUT=30000
# NAVIGATION_TIMEOUT=30000
